package pro.sky.java.course2.springcoredemo.services;

import org.springframework.boot.autoconfigure.SpringBootApplication;
import pro.sky.java.course2.springcoredemo.controllers.PersonController;
import pro.sky.java.course2.springcoredemo.controllers.WeightController;

@SpringBootApplication
public class SpringCoreFakeApplication {
    public static void main(String[] args) {
//        String serviceName = "pro.sky.java.course2.springcoredemo.PersonServiceImpl";
//        PersonService personService = new PersonServiceImpl(new PerfectWeightCalculatorImpl());
//        PersonController personController = new PersonController(personService);
//        WeightController weightController = new WeightController(personService);
    }
}
